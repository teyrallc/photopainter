# Pinned for reproducible installs. The app already carries a Pillow-compat
# shim because Pillow's API moved between versions, so leaving these unpinned is
# a real risk. Regenerate from a known-good device with `pip freeze` when
# bumping. On a Pi Zero 2 W these resolve from piwheels.
Flask==3.0.0
Werkzeug==3.0.1
Pillow==10.2.0
numpy==1.26.4
# CLI image tooling (src/display_picture.py imports cv2). headless = far lighter
# on a Pi Zero than full opencv-python. Saliency crop degrades gracefully if the
# contrib saliency module is absent.
opencv-python-headless==4.9.0.80
spidev==3.6
RPi.GPIO==0.7.1
# libgpiod v2 binding for the physical button controller (src/display_buttons.py).
gpiod==2.1.3
qrcode[pil]==7.4.2
pyngrok==7.1.6
