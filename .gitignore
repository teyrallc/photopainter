output/*.png
output/*.jpg
output/*.jpeg
models/
OnnxStream/
venv/
__pycache__/
*.pyc
*.pyo
.env

# Runtime device configuration — holds the WiFi password, admin password hash
# and session secret, so it must never be committed.
config.json
